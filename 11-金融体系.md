# 11 · 金融体系（债券 · 会计费 · 银行 · 财报）

> 来源：`formulas_bonds.md`（🟢）、`registry/finance-accounting-lift.json`（🟢 verified）、
> `registry/p0-finance-statements.json`（🟢 verified_bundle_static）、`server/game/loans.ts`（⚪）。

---

## 一、债券（Bonds）

### 1.1 基本参数（🟢）

| 项 | 值 |
|---|---|
| 面值 `go` | **$5,000 / 单位** |
| 解锁等级 | **10** |
| 利率区间 | **0.5% – 2.0%**，步长 **0.1%**（两个 realm 一致） |
| 赎回锁定期 | **购买后 14 天** |
| 评级档 | 18 档（AAA … D），UI 分 7 个筛选组 |

### 1.2 关键公式（🟢）

```
每日利息 = floor( amount × 50 × interestRate% )
         例：100 张 × 1.5% → floor(100×50×1.5) = $7,500/天

期间利息 = floor( amount × 5000 × interestRate% ) / 100

可发行上限 = max(0, floor( Σ(building.cost × building.size) / 5000 ) − 已售出张数 )
           ← 建筑总价值就是抵押品，能发的债不超过建筑价值

利率调整   = round(currentRate × 10 + step) / 10       // 0.1% 步进
```

### 1.3 评级 18 档与 7 个筛选组（🟢）

| 筛选组 | 包含评级 |
|---|---|
| AAA to AA | AAA, AA+, AA |
| AA- to A | AA-, A+, A |
| A- to BBB | A-, BBB+, BBB |
| BBB- to BB | BBB-, BB+, BB |
| BB- to B | BB-, B+, B |
| B- to C | B-, C |
| D to D | D |

🔵 评级机构的具体算法官方不公开，只说明会考虑：是否每日经营、资产价值、持有债券、债券负债、
现金流、公司等级、证书、历史是否按时付息。**违约过的公司，坏评级持续更久。**

### 1.4 购买限制（🟢）

| 买方等级 | 单次最多买发行方总量的 |
|---|---:|
| 1–14 | **1%** |
| 15–19 | **3%** |
| 20+ | **5%** |

另：不能买关联公司的债、不能买自己的债（自己看到的是"调整"按钮）。
同一发行方、同一利率的多次购买会**合并成一笔持仓**。

### 1.5 违约与重组（🟢 概念 / 🔵 数值）

```
发行方违约：  始终按【借入价值的 100%】计（8 月 1 日更新后）
              评级下调且持续更久；损益表记 "Write offs"（对违约方是收益）
持有方：      至少收回 restructure_percentage 比例的面值
              损益表记 "Defaults"
新发行的债：  restructure_percentage = 0
              🔵 该比例随时间上升，上升规则在服务端
```

流水类型：`RESTRUCTURED_BOND_CALL`（"Restructured {percent}% bond call"）、
`INSOLVENCY_BOND_CALL`、`BOND_LIQUIDATED`、`BOND_INTEREST`、`BOND_YIELD`、`BOND_CALL`、
`BOND_INVESTMENT`、`BOND_SALES`。

每张持仓还跟踪 `interestCollected`、`missed_payments`、`purchased_at`。

### 1.6 债券 API（🟢）

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/api/bonds/` | 市场上全部债券 |
| GET | `/api/bonds/rating/{rating}/` | 按评级筛选 |
| GET | `/api/bonds/{id}/` | 单个债券 |
| PATCH | `/api/bonds/` body `{amount, interest}` | **发行/调整**（amount = 现金 / 5000） |
| PATCH | `/api/bonds/{id}/` body `{amount, interest, buyer:"me"}` | **购买** |
| PUT | `/api/bonds/{id}/` body `{amount}` | **赎回**（issuer 回购，付全额面值） |
| GET | `/api/v2/companies/{id}/bonds/owned/` | 持有的 |
| GET | `/api/v2/companies/{id}/bonds/sold/` | 发行的 |

### 1.7 拆楼的抵押品底线（⚪ 私服）

`domain/buildings/building-rules.ts`：拆除后剩余建筑价值不得低于未偿债券负债的 **80%**，
否则拒绝拆除（`BOND_COLLATERAL_FLOOR = 0.8`）。**官方没有这条**，是私服加的保护。

---

## 二、会计费用与银行（🟢 verified）

这是很多人不知道的一条：**现金和债券持仓超过免征额之后要交会计费。**

```
免征额 = 3,000,000                          // Xcr = 3e6，基准
       + cfoAccountingSkill × 500,000       // 高管抬升 executiveLift
       + cfoAccountingSkill × bankSize × 50,000   // 银行抬升 bankLift

计费基数 = 现金 + 债券持仓
```

### 银行（建筑 `n`）

| 项 | 值 |
|---|---|
| dbLetter | `n`（分类 other） |
| costUnits | 40 → 兜底造价 $138,000/级 |
| buildDuration | 28,800 秒（8 小时） |
| salaryModifier | **0** |
| 等级上限 | **40**（bundle `gst`） |
| 面板显示 | "{size × 10}% boost" |
| **每公司只能有一座银行** | |
| 生效条件 | 必须**已放置且不在建造中**（否则显示 `bankNotContributing`） |

> **CFO 的会计技能才是驱动这条的，不是 COO。**（`registry/executives-skills.json` 特意点名纠正）

流水细节字段：`bank_level`、`bank_lift`、`executive_lift`、`bank_not_contributing_reason`。

---

## 三、财务报表科目（🟢 verified_bundle_static）

### 资产负债表 `GET /api/v2/companies/:id/balance-sheet/`
```
date, cash, cashReservedForOrders, accountsReceivable, workInProcess, materials,
research, finishedGoods, valuationAllowance, deposits, investmentInBonds,
buildings, constructionInProgress, patents,
bondsPayable, contributedCapital, retainedEarnings
```

### 损益表 `GET /api/v2/companies/:id/income-statement/`
```
date, dateFrom, sales, cogs, freightOut, constructionCosts, marketFees,
salariesCosts, trainingCosts, poachingCosts, gameIncome, executiveRoyalties,
gainOnSale, patentConversion, bondDefaults, bondWriteoffs, accountingOverhead,
bondInterestExpense, bondInterestIncome, donations, otherComprehensiveIncome,
netIncome, economicValueAdded, cashAllExpenses
```

### 关键比率（🟢）
```
利息保障倍数 = totalExpenses / (−bondInterestExpense)
负债/建筑    = bondsPayable / buildings
```

### 现金流分类
债券相关流水归到合并类别 `[BONDS, BOND_DEFAULTS, OWN_BONDS]`。

---

## 四、贷款（⚪ 完全是私服自造）

`server/game/loans.ts` 的注释自己写着 "No in-game reference exists yet"。

```
额度上限 = 2 × 公司等级 × 50,000        // 5 级 → $500,000
默认利率 = 10%
期限     = 7 天
```

**官方 Sim Companies 没有这个贷款系统**，融资只有债券。写模拟器时不要把它当官方机制。

---

## 五、支持者折扣（⚪ 私服）

`server/game/company.ts`：`SUPPORTER_DISCOUNT_PERCENT = 10`、`SUPPORTER_DURATION_DAYS = 30`。
