# Sim Companies 2026 版机制算法文档

> **数据基准**：`server/data/decompile/` —— 2026-05-27 官方前端 bundle 快照
> （`www.simcompanies.com` HAR → Wakaru 解包 → 人工提取），前端版本标记 `v25.0`。
> **未使用** `historical/2019/`、`historical/2022/` 里的任何旧数据。
>
> 全部内容来自你本机 `phantom-backend-7x-master` 仓库里已有的文件，没有联网、没有额外抓包。

---

## 一、这套文档解决了什么

你之前那份《游戏算法·阶段 0·现状清点》里列了 5 个未定量。这份文档里，**其中 4 个已经有确定答案**：

| 阶段 0 的未定量 | 现在的答案 | 出处 |
|---|---|---|
| ① 工资单位是每小时还是每天？ | **既不是小时也不是天，是 "per tick"**：`wages = 345 × salaryModifier × size`，作为单价直接进单位成本 `OH(建筑)/每小时产量`。官方百科截图交叉验证过（苹果：$0.51/单位） | `formulas_admin.md §7/§13`、`registry/encyclopedia-formulas.json#encyclopedia-production-worker-admin-cost` |
| ② 行政开销分母 | **不是客户端算的**，服务端算好后由 `GET /api/v2/companies/me/administration-overhead/` 下发；客户端只做 COO 减免。建筑点数 `i = Σ size×100`（排除 `n/3/4/5`） | `formulas_admin.md §1–§4` |
| ③ 品质 Q0–Q12 影响什么 | 采矿类影响**产量**（×quality/100）；其余只影响**零售速度/售价与交易所撮合优先级**；累加器类每级 +bonusPerQuality% 速度 | `formulas_production.md §3`、`research_tree.json qualityEffects` |
| ④ 零售价格/客流怎么影响销量 | 完整闭式公式已还原（`kle → J7r → e9r/t9r/r9r`），含饱和度、品质、二次价格惩罚、天气 | `formulas_retail.md §3`、`registry/retail-duration.json` |
| ⑤ 产品 #146 的 ph | 源数据 `resources.json` 里 **Pumpkin(146) `producedPerHourRaw` 明确写在表里**，见《01-资源产品全量数据》 | `resources.json` |

> 品质上限不是 Q0–Q5，是 **Q0–Q12**（`maxQuality: 12`）。

---

## 二、可信度分级（每条结论都会标）

| 标记 | 含义 |
|---|---|
| 🟢 **官方确证** | 直接来自 2026 官方前端 bundle 的反编译，有 `chunk_xxx.js:line` 或 bundle byte offset 作证据；部分还有官方页面截图数值交叉验证 |
| 🟡 **官方数据 + 推断** | 常量来自官方 bundle，但组合方式/单位由重建者推断；两份来源文件之间偶有措辞冲突 |
| 🔵 **服务端不可见** | 官方把这段逻辑放在服务端，前端 bundle 里根本没有；本文档给出的是私服的**自定实现**，不代表官方 |
| ⚪ **私服自造** | 官方没有这个东西，是这个私服项目为了跑起来自己加的 |

**看到 🔵 和 ⚪ 就别拿去当官方公式用。** 这两类在文档里都单独标出来了。

---

## 三、全局核心常数（🟢 官方确证）

来源（分散在多个文件，逐条标注）：
`Ji` 段在 `resource_lookups.json`；`Zor` / `isr.B` / `qp` / 4% 手续费在 `formulas_market.md`；
`UK` / `E0` / `K0` 在 `formulas_admin.md`；研究品质上限与 6.25% 在 `research_tree.json`。

| 常量 | 值 | 含义 |
|---|---:|---|
| `AVERAGE_SALARY` | **345** | 基准工资，几乎所有钱的公式都以它为单位 |
| `SALARY_MID` | `{0: 655, 1: 700, 2: 745}` | 三种经济状态下的工资中位数（0 衰退 / 1 正常 / 2 繁荣） |
| `ROBOT_COST` | **940** | 机器人参考单价 |
| `Zor` | **370** | 零售经济模型基础缩放因子 |
| `isr` | `{ B: 2.28 }` | 建筑类型的零售模型修正，只有销售办公室 `B` 有 |
| `RETAIL_MODELING_QUALITY_WEIGHT` | **0.3** | 零售模型里品质的权重 |
| `G9`（季节阈值） | **0.8** | 饱和度 > 0.8 才算"当季" |
| `MIN/MAX_WEATHER_SPEED_MULTIPLIER` | **0.3 / 1.7** | 天气对销售速度的上下限 |
| `PRODUCTION_SPEED_MODIFIER_DAYS` | **21** | 生产加速持续天数 |
| `PRODUCTION_SPEED_MODIFIER_RESOURCES` | **3** | 同时可加速的资源数 |
| `LAUNCH_QUEUE_MAX` | **30** | 发射队列上限 |
| `TUTORIAL_BONUS` | **12000** | 教程奖励金 |
| `PURCHASE_SIMBOOSTS_FEE` | **0.1** | SimBoost 购买费率 |
| `TAG_EXPIRY_DAYS` | **3** | 买卖标签过期天数 |
| `UK / E0`（技能软上限拐点） | **80 / 60** | 高管技能衰减拐点 |
| `K0`（管理员上限） | **12** | `maxAdmins = min(floor(建筑点数/4), 12)` |
| 交易所手续费 | **4%** | 两个 realm 统一 |
| 建造材料系数 `qp` | `{101:4, 102:55, 108:16, 111:1}` | 每 costUnit 需要：4 钢筋混凝土(101) / 55 砖(102) / 16 木板(108) / 1 建造单位(111) |
| `研究品质上限` | **12** | |
| `基础专利概率` | **6.25%** | 每点研究 |

---

## 四、⚠️ 源文件之间的三处冲突（重要）

我在整理时发现，`formulas_*.md` 里有几处**建筑名字是猜的、猜错了**。数值是对的，名字别信。

### 冲突 1：建筑字母 → 名称的对应表

`formulas_production.md §8` 和 `formulas_admin.md §7` 都列了一张 "字母 / 建筑名 / salaryModifier" 表，
两张表**互相矛盾**（同一个 `G`，一个说 Greenhouse，一个说 Gold Mine），而且**都和权威数据对不上**。

**权威是 `buildings.json`**（它带 `dbLetter` + `name` + `category` 三元组，且被服务端 `game-data/buildings.ts` 直接加载）：

| 字母 | 两份 md 猜的名字 | ✅ 真实名字（buildings.json） | salaryModifier（三处一致） |
|:-:|---|---|---:|
| `P` | Plantation / Plantation | **farm 农场** | 0.3 |
| `W` | Well / Water Reservoir | **water_reservoir 蓄水池** | 1.0 |
| `E` | Power Plant / Electric Plant | **power_plant 发电厂** | 1.2 |
| `O` | Orchard / Oil Rig | **oil_rig 石油钻井** | 1.5 |
| `G` | Greenhouse / Gold Mine | **grocery_store 食品店** | 0.4 |
| `C` | Chemistry Lab / Copper Mine | **electronics_store 电子产品店** | 0.5 |
| `n` | Nuclear Power Plant / Headquarters | **bank 银行** | 0.0 |
| `3/4/5` | Hydro/Geo/Coal 或 HQ2/3/4 | **castle 城堡 / park 公园 / lake 湖泊**（全是休闲建筑） | 0.0 |
| `I` | Restaurant / Ice Cream | **spring_market 春季市集** | 0.7 |
| `r` | Space Station / Restaurant | **restaurant 餐馆** | 1.7 |

**结论：`salaryModifier` 的数值全都对，名字全按 `buildings.json` 走。**

顺带修正一条重要理解：`formulas_admin.md §1` 说被排除出行政开销的 `v$ = ["n","4","5","3"]` 是"总部 HQ1–4"。
**不是。** 那是**银行 + 三种休闲建筑**（城堡/公园/湖泊）。它们 `salaryModifier = 0`，不产生建筑点数。

### 冲突 2：零售建筑 → 可售商品的对应

`formulas_retail.md §1` 的名字列同样是猜的（把 `A` 写成 Electronics Store，其实卖的是 11/12 汽油柴油，是**加油站**）。
**权威是 `resource_lookups.json` 的 `dpr.categories` + `registry/seasonal.json`**，服务端 `game-data/retail.ts` 与之逐条对齐：

| 字母 | ✅ 真实建筑 | 可零售资源 ID（`dpr` 权威） |
|:-:|---|---|
| `G` | 食品店 Grocery store | 3,4,5,7,8,9,119,122,123,124,125,126,127,140,**152** |
| `A` | 加油站 Gas station | 11,12 |
| `C` | 电子产品店 Electronics store | 24,25,26,27,28,**98** |
| `2` | 汽车经销店 Car dealership | 53,54,55,56,57 |
| `H` | 时装店 Fashion store | 60,61,62,63,64,65,70,71 |
| `d` | 五金店 Hardware store | 102,103,108,109,110 |
| `B` | **销售办公室 Sales office（航天）** | 91,94,95,96,97,99 |
| `r` | 餐馆 Restaurant | 117,119,121,122,123,124,125,126,129,130,131,132,134,142,143,149 |
| `t` | 秋季/万圣节市集 | 146,147,148 |
| `u` | 圣诞市集 | 67,144,150 |
| `z` | 海滩市集 | 153,154 |
| `I` | 春季/复活节市集 | 151,155 |

> **附带发现**：私服 `server/game-data/retail.ts` 里的 `RETAIL_PRODUCTS` 与官方 `dpr` 有 4 处出入 ——
> `G` 里把 152 换成了 153/154/155；`C` 少了 98；`B` 多了 98；`I` 多了 152。
> 如果要 1:1 复刻官方，**以 `dpr` 为准**。

### 冲突 3：零售核心函数 `kle` 返回的到底是什么

- `formulas_retail.md §3` 说返回**卖完 N 件需要的小时数**
- `formulas_market.md §9` 说返回**每小时卖出件数**

**以 `registry/retail-duration.json`（标记 `confidence: verified`）为准：返回的是"卖完所需时间"（秒）**，
服务端 `calculateRetailDuration` 与客户端 `kce/u$r/f$r` 链是同一个公式。
`formulas_market.md §9` 那份是误读。另有 `Kve()` 包了一层做 `100 × 3600 / u` 才得到"每小时件数"。

### 冲突 4：建造材料 101 / 108 的名字被 `buildings.json` 标反了

`buildings.json _meta.upgradeFormula.constructionResourcesPerCostUnit` 里写着
`101: planks`、`108: concrete`、`111: steel_beam`。**这三个标注全是错的。**

对照 `resources.json`（权威）：

| ID | ✅ 真实名称 | `buildings.json _meta` 的错误标注 | qp 系数 |
|---:|---|---|---:|
| 101 | **Reinforced Concrete 钢筋混凝土** | planks 木板 ❌ | 4 |
| 102 | Bricks 砖 ✅ | bricks | 55 |
| 108 | **Planks 木板** | concrete 混凝土 ❌ | 16 |
| 111 | **Construction Units 建造单位** | steel_beam 钢梁 ❌ | 1 |

**每 costUnit 实际要：4 份钢筋混凝土 + 55 块砖 + 16 块木板 + 1 个建造单位。**

### 冲突 5：`formulas_aerospace.md` 的配方原料名几乎全错

那份文件里航天配方的**数量全对，原料中文/英文名大面积错位**
（把 16 硅写成 Timber、17 化学品写成 Planks、18 铝写成 Steel、22 电池写成 Reinforced Concrete、
43 钢写成 Aluminum、69 金条写成 Processor、74 甲烷写成 Petroleum…）。
**一律以 `resources.json` 的 `producedFrom` 为准**，见《06》第二节（已按权威数据重写）。

### 冲突 6：`formulas_admin.md` 对 `ade()` 的旁注自相矛盾

`ade(i) = min(floor(i / 4), 12)`，而 `i = Σ size × 100`。
所以**一栋 1 级楼（100 点）就已经 `floor(100/4) = 25 > 12`，直接顶满 12 个管理员**。
该文件旁注写的"1 admin per 400 building points"与公式对不上，不要采信旁注。

---

## 五、文档清单

| 文件 | 内容 |
|---|---|
| `00-总览与来源说明.md` | 本文 —— 来源、可信度分级、全局常数、源文件冲突 |
| `01-资源产品全量数据.md` | 151 种资源全字段 + 配方 + 经济模型 |
| `02-建筑全量数据.md` | 54 种建筑全字段 + 造价/升级/工资/机器人公式 |
| `03-生产机制与公式.md` | 完整生产链公式、采矿品质、机器人、累加器、事件、季节 |
| `04-零售机制与公式.md` | 零售核心公式全推导、季节、天气、饱和度、最优定价 |
| `05-餐馆机制.md` | 餐馆全套：座位、菜单、食材系数、评分、上座率、周期、装修 |
| `06-销售办公室与航天.md` | 找客户、合同、火箭发射、爆炸率、专利 |
| `07-高管学院与行政开销.md` | 技能叠加与软上限、COO/CFO/CMO/CTO、挖角、学院、培训 |
| `08-研究与品质体系.md` | 12 研究线、9 研究室、专利阈值、品质效果 |
| `09-市场与交易所.md` | 价格 Tick 表、手续费、撮合、运输、买卖单 |
| `10-政府订单.md` | tier、投标、押金、奖励、履约 |
| `11-金融体系.md` | 债券、贷款、财报科目、比率 |
| `12-等级成就与证书.md` | 等级 tier 表、能力解锁、XP、成就、证书、丰度 |
| `13-SimBoost与全局加成.md` | SimBoost 用途、生产/销售加成、休闲加成、加速 |
| `14-其他机制汇总.md` | 报纸、收藏品、礼品篮、建筑拍卖、机器人、天气、Realm 阶段 |
| `资源全量表.csv` | 151 行 × 22 列，可直接进 Excel |
| `建筑全量表.csv` | 54 行 × 20 列 |
| `经济模型参数表.csv` | 151 资源 × 3 经济状态 |
