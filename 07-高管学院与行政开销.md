# 07 · 高管、学院与行政开销

> 来源：`formulas_admin.md`（🟢）、`formulas_merger.md`（🟢）、
> `registry/executives-skills.json` / `executives-academy.json`（🟢 verified）、
> `server/domain/executives.ts`、`server/application/executives/executive-use-cases.ts`。

---

## 一、行政开销（Administration Overhead）

### 1.1 建筑点数（🟢）

```
i = Σ building.size × 100      对所有【非免费/未锁定】且【kind 不在 v$ 里】的建筑

v$ = ["n", "4", "5", "3"]
```
> ⚠️ **`formulas_admin.md` 把 `v$` 注释成"总部 HQ1–4"，这是错的。**
> 对照 `buildings.json`：`n` = 银行 bank，`3/4/5` = 城堡 castle / 公园 park / 湖泊 lake（三种休闲建筑）。
> 这四种建筑 `salaryModifier = 0`，不计入建筑点数。

### 1.2 开销倍数从哪来（🟢）

**客户端不算它**，服务端算好后下发：
```
GET /api/v2/companies/me/administration-overhead/          → 当前 AO
GET /api/v2/companies/administration-overhead-plus-one/    → 多雇 1 个管理员后的 AO
```
存在 Redux 的 `user.administrationOverhead`，是个**倍数**（1.05 = 5% 开销）。

> 🔵 **AO 本身的生成公式官方不可见**，只知道输入是建筑点数和管理员数量。
> 这正是你阶段 0 文档里 `MAP_ADMIN_PER` 那个分母 —— 它在官方那边也是服务端的，
> 只能靠"记下总等级 + 游戏显示的百分比"反解。

### 1.3 COO 减免（🟢）

```
ph(AO, skillCOO) = AO − (AO − 1) × skillCOO / 100
```
例：AO = 1.10（10% 开销），COO 有效技能 40 →  `1.10 − 0.10 × 0.4 = 1.06`（6% 开销）。
**COO 有效技能 100 时开销完全归零。**

> 这就是你阶段 0 文档里强调的"百分比的百分比"：
> 开销 10%、技能 40 → 结果是 6%，不是 10% − 40%。**你那条实现是对的。**

### 1.4 边际成本与管理员上限（🟢）

```
再雇一个管理员的成本 = (AO_plusOne − 1) × (i + 100) − (AO − 1) × i
管理员上限          = min( floor(建筑点数 / 4), 12 )
```
> ⚠️ `formulas_admin.md` 旁注写"1 admin per 400 building points"，**和公式对不上**：
> `i = Σ size × 100`，一栋 1 级楼就是 100 点 → `floor(100/4) = 25 > 12`，**开局第一栋楼就顶满 12 个管理员**。
> 以公式为准，别信旁注。

### 1.5 每栋楼摊到的开销（🟢）

```
每栋开销   = size × 100 × (AO − 1)
COO 后实际 = size × 100 × (AO − 1) × (1 − skillCOO/100)
单位管理成本 = max(0, ph(AO, skillCOO) − 1) × 单位人工成本
```

---

## 二、高管技能计算（🟢 —— 全文档最关键的一条）

### 2.1 岗位匹配倍率（`BT`）

```
rawSum = Σ  exec.skills[role] × positionMultiplier
```
| 该高管的岗位 vs 要算的 role | 倍率 |
|---|---:|
| **完全匹配**（COO 算 coo） | **×1.0** |
| **该 role 的学徒**（COO Apprentice 算 coo） | **×0.5** |
| **其它高管岗位** | **×0.25** |
| 非高管 | ×0 |

### 2.2 软上限（`s5t`）—— **先加总，再衰减**

```
s5t(x) = x                    ,  x ≤ 60
       = 60 + (x − 60)/2      ,  60 < x ≤ 80
       = 70 + (x − 80)/4      ,  x > 80        ← 注意是 /4，不是 /2

常量：E0 = 60, UK = 80
```
> **注意第三段**：`80 + (x−80)/2` 要先经过第二段的压缩，
> 展开后是 `70 + (x−80)/2`。源码是**顺序执行两个 if**，不是互斥分支：
> ```js
> if (e > 80) e = 80 + (e - 80) / 2;
> if (e > 60) e = 60 + (e - 60) / 2;
> ```
> 代入 x = 100：先 `80 + 10 = 90`，再 `60 + 15 = 75`。
> 所以第三段的正确闭式是 **`s5t(x) = 70 + (x − 80)/4`**（x > 80）。
> `formulas_admin.md` 里写的 `70 + (x−80)/2` 是**展开时漏了一次除 2**，别照抄。
> 你阶段 0 文档里写的 `60 + 10 + (p−80)/4` —— **是对的**，和源码一致。

| 原始总点数 | 有效技能 |
|---:|---:|
| 40 | 40 |
| 60 | 60 |
| 80 | 70 |
| 100 | 75 |
| 120 | 80 |
| 160 | 90 |

### 2.3 最终

```
effectiveSkill = floor( s5t( BT(executives, role) ) )
```

> **顺序不能反**：必须"先把所有高管的贡献加总，再对总和做衰减"。
> 每人各自衰减再相加，在总点 80 那里会给出 80（正确答案是 70），小数值下两种写法结果相同，
> 所以不写测试发现不了。（这条你阶段 0 文档里已经钉过测试了，和官方一致。）

### 2.4 四个岗位各管什么

| 岗位 | 技能字段 | 作用 |
|---|---|---|
| **COO** | management 管理 | 减免行政开销：`AO − (AO−1)×skill/100`；也决定生产排队的 spareCapacity |
| **CFO** | accounting 会计 | 会计费用减免（`registry/finance-accounting-lift.json`；注意**不是 COO**） |
| **CMO** | communication 沟通 | 销售加成 `+floor(skillCMO / 3)` 百分点；餐馆评分也吃这个 |
| **CTO** | science 科学 | 研发建筑产量 `×(100 + skill×2)/100`；专利概率 `6.25% × (1 + skill/100)` |

---

## 三、学院 Academy（建筑 `y`）🟢 verified

```
建筑参数：costUnits 19（$65,550/级）, buildDuration 28,800 秒（8h）, salaryModifier 0, sincePhase 5
等级贴图 tiers = [1, 5, 10, 15, 20]
```

### 3.1 有效学院等级

```
active = Σ 所有【不忙】且【不在地标位（position 不以 'l' 开头）】的学院的 size
slots  = Σ size（扩建中的按 size − 1 计）
```

### 3.2 学徒位解锁（`Gu = 5`）

| 学院累计等级 | 解锁 |
|---:|---|
| 5 | COO 学徒 |
| 10 | CFO 学徒 |
| 15 | CMO 学徒 |
| 20 | CTO 学徒 |

1 级学院正确显示"学徒位已锁"。

### 3.3 ⚠️ 官方在这里没给数值

`registry/executives-academy.json` 明确写着：

> 原版客户端**无条件**渲染 `Building.academyImpactNotDocumented`（"具体影响未做说明"），
> **原版里根本没有数值公式。**

私服采用的规则（⚪ 私服自定，代码里明确注明是 canonical choice 而非官方）：
```
每 5 级有效学院：训练 +1 技能点、招聘起始技能 +1，上限 +2
academySkillBonus(activeLevels) = min(2, floor(activeLevels / 5))
训练收益 skillGain = 1 + academySkillBonus(active)
学徒起始技能 = 相关技能的 50%（bundle mPt）        ← 这条是 🟢 官方
```

---

## 四、招聘、猎头与挖角（🟢）

### 4.1 中介档次

| 中介 | 枚举 | 费用倍率 `Joe` | 候选人数 `usr` |
|---|---:|---:|---:|
| In-House 自有 | 1 | **0×** | 0 |
| Staffing Agency 人力中介 | 2 | **0.5×** | 8 |
| Good Agency 优质中介 | 3 | **2×** | 4 |
| Top Talent Agency 顶级猎头 | 4 | **5×** | 3 |

```
中介费 = Joe[agency] × expectedSalary
```

### 4.2 报价区间

```
0.9 × expectedSalary  ≤  offer  ≤  10 × expectedSalary
```

### 4.3 反挖角（雇主侧）

```
留人门槛 $or = 75
requiredSalary = ceil(expectedSalary × 75 / 100)
```
只有把现薪提到期望薪的 **75% 以上**，才能触发反报价按钮。

### 4.4 关键常量

| 常量 | 值 | 含义 |
|---|---:|---|
| `$or` | 75 | 反报价门槛（%） |
| `$At` | 86,400 | 挖角报价响应期限 = 24 小时 |
| `$K` | 5 | 调查对方公司花 5 SimBoost |
| `KO` | 30,000 | 单次培训费 $30,000 |

| `Uor` | 1 | 遣散费公式分子 |
| `ije` | 3 | 主动解雇遣散 = 薪水 × 3 |
| `mfe` | 3 | 退休预告 3 天 |
| `BAt` | 6 | 培训历史窗口 6 周 |
| `_$` | 97,200 | 培训窗口 27 小时 |
| `b$` | 10,800 | 🟡 3 小时（源文件只写 "executive-related timing"，"入职适应"是推断） |
| `y$` | 36,000 | 🟡 10 小时（源文件只写 "executive-related timing"） |
| 挖角冷却 | 4 天 | 刚接受/拒绝过报价的高管，中介不会再盯上 |

> ⚪ 另有一个 $10,000 的培训档位（私服 `domain/executives.ts` 注释称对应客户端常量 `gPt`），
> 但 **decompile 目录里查不到 `gPt`**，不要当官方数据用。

### 4.5 离职补偿

```
USr(expectedSalary, totalDaysActive) = floor( expectedSalary × totalDaysActive / 200 )
在职不足 2 天 → 0
```

### 4.6 报价状态机

```
ru.FOUND      中介找到人，尚未正式报价
ru.STANDING   已正式报价，等待答复
ru.OUTDATED   人已接受别家，本报价作废
ru.REFUSED    被拒
```

```
猎方 POST my-offers        → FOUND
猎方 PATCH {executive, salary} → STANDING，雇主收到通知
                                    ├─ 反报价（涨到 ≥75%）→ 人留下
                                    ├─ 放人 → 人走，雇主拿补偿
                                    └─ 拒绝 → 人留下但以后可能还会走
```

### 4.7 高管资历

`e7.JUNIOR` / `e7.EXPERIENCED` / `e7.SENIOR`

### 4.8 通知类型（🟢）

`EXECUTIVE_OFFER` / `EXECUTIVE_ACCEPTED_OFFER` / `EXECUTIVE_WANTED_TO_ACCEPT_OFFER`（想接受但你公司现金不足）/
`EXECUTIVE_DECLINED_OFFER` / `EXECUTIVE_STAYED` / `AGENCY_FOUND_EXECUTIVE` / `AGENCY_FAILED` /
`EXECUTIVE_TRAINING_FINISHED` / `EXECUTIVE_WILL_RETIRE` / `EXECUTIVE_RETIRED` /
`EXECUTIVE_BURNOUT` 倦怠 / `EXECUTIVES_STRIKE` 罢工

---

## 五、⚠️ 官方没有"公司合并"这回事

`formulas_merger.md` 开头就澄清了：
> Sim Companies **没有公司合并/收购系统**。UI 上叫 "Merger & Acquisition" 的东西，
> 实际上就是**高管挖角（Hostile Offers / Executive Poaching）**，名字是历史遗留。

---

## 六、高管相关 API 一览（🟢）

| 侧 | 方法 | 路径 |
|---|---|---|
| 猎方 | GET/POST/PATCH/PUT/DELETE | `/api/v2/companies/executives/my-offers/[{id}/]` |
| 雇主 | GET/PATCH/PUT/DELETE | `/api/v3/companies/executives/hostile-offers/[{id}/]` |
| 管理 | GET/PATCH/DELETE | `/api/v4/executives/{id}/` |
| 招聘 | POST | `/api/v4/executives/{companyId}/` |
| 培训 | POST / PATCH / DELETE | `/api/v4/executives/trainings/{execId}/[{trainingId}/]` |
