# 09 · 市场与交易所

> 来源：`formulas_market.md`（🟢）、`server/domain/market/market-rules.ts`、
> `registry/market-recent-resources.json`。

---

## 一、价格 Tick 表（🟢 官方 `U0` / `Vor`）

挂单价格**必须落在 tick 网格上**，否则被拒。

| 价格区间 | Tick | `U0` 返回 |
|---|---:|---|
| ≥ 20,000 | **500** | `[500, 1]` |
| 10,000 – 19,999.99 | 100 | `[100, 1]` |
| 5,000 – 9,999.99 | 25 | `[25, 1]` |
| 1,000 – 4,999.99 | 10 | `[10, 1]` |
| 500 – 999.99 | 5 | `[5, 1]` |
| 200 – 499.99 | 2 | `[2, 1]` |
| 100 – 199.99 | 1 | `[1, 1]` |
| 50 – 99.99 | 0.5 | `[1, 2]` |
| 20 – 49.99 | 0.25 | `[1, 4]` |
| 5 – 19.99 | 0.1 | `[1, 10]` |
| 2 – 4.99 | 0.05 | `[1, 20]` |
| 1 – 1.99 | 0.01 | `[1, 100]` |
| 0.5 – 0.99 | 0.005 | `[1, 200]` |
| < 0.5 | **0.001** | `[1, 1000]` |

服务端校验：`|price/tick − round(price/tick)| > 1e-6` 即拒绝（`PRICE_TICK_INVALID`）。

---

## 二、手续费（🟢）

```
fee = ceil( amount × price × 0.04 )        // 两个 realm 都是 4%
```
- **只从卖方那边扣**，买方付 `amount × price`
- 只在**成交时**收，挂单和撤单都不收

> 浮点陷阱：`25 × 0.04 = 1.0000000000000002`，`ceil` 会给出 2。
> 私服的做法是先 `round(x × 1e6)/1e6` 再 `ceil`。

---

## 三、卖单利润估算（🟢）

```
revenue        = amount × price
sourceCost     = amount × unitSourceCost            // 加权平均生产成本
transportUnits = ceil( amount × resource.transportation )
transportCost  = transportUnits × transportationUnitPrice
fee            = ceil( amount × price × 0.04 )
estimatedProfit= revenue − sourceCost − fee − transportCost
```

`unitSourceCost` = 该批货的总生产成本 ÷ 当前库存量（**加权平均**）。

> 这印证了你阶段 0 文档里"仓库按批次记、按数量加权平均"的做法 —— 官方就是这个口径。

---

## 四、运输（🟢）

```
交易所订单： transportUnitsNeeded = ceil( amount × resource.transportation )
合同直销：   transportUnitsNeeded = ceil( amount × resource.transportation / 2 )   ← 减半
```
运输额度不够就挂不上单。各资源的 `transportation` 见《01-资源产品全量数据》。

| 例 | transportation |
|---|---:|
| Power (1) | 0 |
| Grain (6) / Eggs (9) | 0.1 |
| Apples (3) | 1 |
| Gold Ore (68) | 10 |
| Sub-Orbital Rocket (91) | 20 |
| Orbital Booster (92) / Starship (93) | 100 |
| **BFR (94)** | **2000** |

---

## 五、行情 Ticker（🟢 接口 / 🔵 算法）

```
GET /api/v3/market-ticker/{realmId}/     → [ { kind, price }, ... ]
```
- 存在 Redux `state.market.ticker[realmId]`
- 建筑造价估算、百科利润计算器全用它

> 🔵 **参考价（reference price）怎么生成，官方 bundle 里没有任何公式。**
> `registry/encyclopedia-formulas.json` 明确写：官方文章说参考价来自"过去 24 小时的市场交易活动"，
> 但赋值算法在服务端，客户端只是 `filter(kind).price` 取第一条。
> **不要从一个价格去反推算法。**

---

## 六、下单接口（🟢）

| 用途 | 方法 | 路径 |
|---|---|---|
| 挂卖单 | POST | `/api/v2/market/order/` body `{resourceId, price, quantity, quality, kind}` |
| 撤卖单 | DELETE | `/v2/market/order/cancel/{id}` |
| 即时买入（吃单） | POST | `/v2/market/take/` body `{resource, quantity, quality, maxPrice, money}` |
| 挂买单 | POST | `/api/market/buy/orders/{resource}/{quality}` body `{amount, minQuality, price}` |
| 撤买单 | DELETE | `/api/v2/companies/market/buy/orders/get/{resource}/{quality}` |
| 公司订单 | GET | `/api/v2/companies/market/orders/{companyId}` |
| 零售模型数据 | GET | `/api/v4/encyclopedia/resources/retail-info/{realmId}` |
| 资源列表 | GET | `/api/v3/resources/{realmId}` |

> P0-08 提示：客户端"买齐缺的建材"那个流程 **不发 `maxPrice`**，只受现金约束。
> 服务端不能强制要求 `maxPrice`。

---

## 七、撮合（🔵 服务端）

客户端只提交、只收结果。已知的：
- **吃单**：服务端撮合现有卖单，`maxPrice` 和 `money` 两个上限取更严的那个，返回 `{moneyDelta, amountBought}`
- **挂卖单**：返回 `{sellOrder, moneyDelta}`，`moneyDelta` 非零说明立刻被现有买单吃掉了一部分
- 价格-时间优先（最低卖价 / 最高买价先撮合）
- 服务端强制 tick 网格

**防自成交（⚪ 私服加的）**：同公司或同玩家的挂单不能自己吃；NPC 供给单（seller `999900`）豁免。

---

## 八、经济状态（🟢 字段 / 🔵 生成）

```
economyState ∈ {0 衰退, 1 正常, 2 繁荣}
```
- 影响 `SALARY_MID`（655 / 700 / 745）→ 直接影响所有生产速度
- 影响每个资源的经济模型参数（见 `经济模型参数表.csv`）
- 通过 WebSocket `newEconomyCycle` 推送（`temporals.economyState`）
- 🔵 状态怎么切换、周期多长，官方不可见

---

## 九、买卖标签（Tags）

`TAG_EXPIRY_DAYS = 3`（🟢）—— 买家/卖家标签 3 天过期。报纸有 `TAGS` 类型文章统计标签数据。
