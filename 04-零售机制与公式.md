# 04 · 零售机制与公式

> 来源：`formulas_retail.md`（🟢）、`registry/retail-duration.json`（🟢 verified）、
> `registry/seasonal.json`（🟢 verified）、`economy_model.json`、`server/game-data/retail.ts`。
>
> ⚠️ 先看《00-总览》冲突 3：`formulas_market.md §9` 把这套公式的返回值读反了，**以本章为准**。

---

## 一、核心公式链（🟢 官方确证 + 服务端 1:1 复刻验证过）

顶层函数 `kle(buildingKind, economyModel, units, salesModifier, price, quality, saturation, acceleration, size, weather)`
**返回：卖完 `units` 件所需的时间**。

### 输入：经济模型（每资源 × 每经济状态）

```
economyModel = {
    buildingLevelsNeededPerUnitPerHour,   // 每 1 件/小时需要多少建筑等级
    modeledProductionCostPerUnit,          // 建模的单位生产成本
    modeledStoreWages,                     // 建模的门店工资（null = 不可零售）
    modeledUnitsSoldAnHour                 // 建模的每小时需求
}
经济状态 economyState ∈ {0 衰退, 1 正常, 2 繁荣}
取法：gk(economyState, dbLetter, quality)
```
全部 151 资源 × 3 状态的具体数值见 `经济模型参数表.csv`。

### 第 1 步 · 饱和度 → 需求乘数

```
d      = clamp(2 − saturation, 0, 2)
demand = max(0.9, d/2 + 0.5)
```
| saturation | d | demand |
|---:|---:|---:|
| 0.03（极度缺货） | 1.97 | **1.485** |
| 0.5 | 1.5 | 1.25 |
| 1.0（平衡） | 1.0 | **1.0** |
| 2.0（严重过剩） | 0 | **0.9**（下限） |

### 第 2 步 · 品质因子

```
qf = quality / 12
```

### 第 3 步 · 零售吞吐量 g

```
g = Zor
  × (buildingLevelsNeededPerUnitPerHour × modeledUnitsSoldAnHour + 1)
  × isr[buildingKind]                       // 只有 B（销售办公室）= 2.28，其余 = 1
  × ( d/2 × (1 + qf × RETAIL_MODELING_QUALITY_WEIGHT) )

Zor = 370,  RETAIL_MODELING_QUALITY_WEIGHT = 0.3
```

### 第 4 步 · 修正后需求

```
adjDemand = modeledUnitsSoldAnHour × demand
```

### 第 5 步 · 最优价格（e9r）

```
optimalPrice = modeledProductionCostPerUnit + (g + modeledStoreWages) / adjDemand
```
**这就是"这个货应该卖多少钱"的解析解。** 服务端 `calculateOptimalRetailPrice` 就是这一行。

### 第 6 步 · 价格弹性 —— 偏离最优价的二次惩罚（t9r）

```
alpha      = (modeledStoreWages + g) / (optimalPrice − modeledProductionCostPerUnit)²
adjRevenue = g − (price − optimalPrice)² × alpha
```
**抛物线**：价格正好等于最优价时 `adjRevenue = g`（最快）；
向上或向下偏离，都按平方掉速度。定高了卖得慢，定低了**也**卖得慢（这条很反直觉，但公式就是对称的）。

### 第 7 步 · 卖完所需时间（r9r）

```
tts秒 = ( units × (price − modeledProductionCostPerUnit) × 3600 − modeledStoreWages )
        / ( adjRevenue + modeledStoreWages )
```

### 第 8 步 · 应用建筑/加成/天气

```
seconds = tts / size / acceleration
seconds = seconds × (1 − salesModifier / 100)
if weather:  seconds /= weather.sellingSpeedMultiplier
```

### 换算成"每小时件数"

```
unitsPerHour = Kve(...) = 100 × 3600 / kle(..., units=100, ...)
```
> bundle 锚点：`function Kve(...){const u=kce(...);return 100*3600/u}`。

---

## 二、⚠️ 一个反直觉但必须照抄的细节（🟢 verified）

`registry/retail-duration.json` 明确写着：

> 客户端是**无条件相除**的。当售价低于建模成本时，分子和分母**同时为负**，
> 相除得到一个**很小的正数**，界面上就显示成"几乎瞬间卖完"。
> **服务端不能加 `numerator > 0` 的保护**，否则算出来的时长会和前端显示的估算值不一致，
> 导致后端拒绝掉前端认为合法的挂单。

这是官方客户端的实际行为，复刻时必须原样保留。

另：前端"最大数量"按钮的上限是 `ceil(modeledUnitsSoldAnHour × 时限 / 3600)` 件。

---

## 三、销售加成的组成（🟢）

```
effectiveSalesMod% = salesModifier          // 公司销售加成（SimBoost / 等级奖励）
                   + recreationBonus         // 休闲建筑加成
                   + floor(skillCMO / 3)     // CMO 有效技能 ÷ 3 向下取整
```

## 四、零售利润 🟢

```
salesModTotal = salesModifier + recreationBonus + floor(skillCMO / 3)
adminOverhead = ph(administrationOverhead, skillCOO)

tts       = kle(建筑类型, 经济模型, 数量, salesModTotal, 价格, 品质, 饱和度, 加速, 等级, 天气)
revenue   = 价格 × 数量
wagesTotal= ceil( tts × wages × acceleration × adminOverhead / 3600 )
profit    = revenue − wagesTotal − cogs
```

---

## 五、季节系统（两套并行，别搞混）🟢

### 5.1 饱和度曲线 `npr` —— 影响**需求**

季节活跃判定 `U6t(resource, date)`：在折线上按日期**线性插值**，`saturation > 0.8` 即当季。

| 季节 | 饱和度折线（mm-dd） |
|---|---|
| **Ramadan 斋月** | 0.03 → (02-18) 0.9 → (03-19) 1.0 → (03-30) 0.03 |
| **Easter 复活节** | 0.03 → (03-20) 0.9 → (04-20) 1.0 → (04-30) 0.03 |
| **Summer 夏季** | 0.03 → (07-14) 1.0 → (08-20) 1.0 → (09-14) 0.03 |
| **Halloween 万圣节** | 0.03 → (10-15) 1.0 → (11-03) 1.0 → (11-15) 0.03 |
| **Xmas 圣诞** | 0.3 → (01-10) 0.03 → (11-25) 0.7 → (12-04) 1.0 → (12-24) 1.0 → (12-31) 0.3 |

### 5.2 季节市集窗口 `Cdr / t5` —— 影响**建筑可用性**（UTC，mm-dd）

| 建筑 | 窗口 |
|---|---|
| `t` 秋季/万圣节市集 | 10-10 → 11-05 |
| `u` 圣诞市集 | 12-01 → 12-27 |
| `z` 海滩市集 | 07-08 → 08-29 |
| `I` 春季/复活节市集 | 04-02 → 04-12 |

### 5.3 ⚠️ 关键：**季节商品不是硬性禁售的**（🟢 verified）

`registry/seasonal.json` 写得很清楚：

> 销售面板**不做季节硬门禁**。过滤条件是 `canSeasonal || RO(resource, now)` ——
> 只要公司有 `capabilities.seasonal`（**等级 ≥ 5**），就能**无条件**看到并挂季节商品。
> 淡季只是通过饱和度表把**需求**压下去（圣诞货 1 月 0.3→0.03，11 月 0.03→0.7…），
> 当季指示灯的阈值是 0.8。
> **把淡季圣诞货做成硬禁售，就和官方行为不一致了。**

需求变化通过 WebSocket 的 `newEconomyCycle` 推送（`temporals.economyState`）下发，没有专门的门禁接口。

---

## 六、天气 🟡

- 只对 `retailSeason === "Summer"` 的资源生效（#153 巧克力冰淇淋、#154 苹果冰淇淋）
- `weather.sellingSpeedMultiplier ∈ [0.3, 1.7]`，在 `kle` 里是 `f /= multiplier`
  → **倍率越大卖得越快**
- 天气对象由服务端按 realm 下发（🔵 生成算法官方不可见）

私服 `game-data/weather.ts` 的自定映射（⚪）：`saturation ≤ 0.8 → 1.0`；`(0.8, 1.0] → 线性 1.0 → 1.7`。

---

## 七、衰减（Decay）

只有两个资源有 `decay = 0.05`：**#153 巧克力冰淇淋、#154 苹果冰淇淋**。
计算函数 `No({kind, amount, datetime}, now)` 给出 `amountConsideringDecay`。
🔵 **具体衰减曲线 bundle 里没展开**，只知道字段和调用点（`chunk_DYe.js:213-218`、`chunk_YWr.js:236-240`）。

---

## 八、零售挂单的忙碌状态（🟢 verified）

```
startRetail 会占用 buildings.busy_until
前端从 busy.category = 's'（SELLING）+ busy.sales_order 渲染"销售中"
busy.sales_order = { image, name, amount, price, remainingProfit, profitAvailableNow }
有 busy 窗口但既无生产也无零售条目 → 回落到 EXPANDING（'b'）
收取：POST /api/v2/order/take/:buildingId/  body {cash}
```

---

## 九、私服的零售定价护栏（⚪ 私服自造，不是官方）

`server/game-data/retail.ts` 的 `getAuthoritativeRetailPrice` 用了一套**简化**的价格区间：
```
qualityMultiplier = 1 + quality × 0.08
baseCost          = modeledProductionCostPerUnit + modeledStoreWages / modeledUnitsSoldAnHour
defaultPrice      = baseCost × 1.25 × qualityMultiplier
maxPrice          = baseCost × 3.0  × qualityMultiplier
minPrice          = max(0.1, baseCost × 0.5)
```
**官方没有这组数**（官方只有第 5 步的 `optimalPrice`）。这是私服为了校验挂单价加的护栏，别当官方公式用。
