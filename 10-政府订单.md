# 10 · 政府订单（Government Orders）

> 来源：`formulas_government.md`（🟢，常量出自 `entry.js:36375-36387` 的 `Rl` 对象）。

---

## 一、系统参数（🟢）

| 参数 | 变量 | 值 | 含义 |
|---|---|---:|---|
| 最少承包商 | `Y5n` | **3** | 一个投标最少 3 家 |
| 最多承包商 | `Z5n` | **7** | 最多 7 家 |
| 投标期 | `K5n` | **5 天** | 从创建到截止 |
| 押金比例 | `X5n` | **0.1（10%）** | |
| 截止小时 | `Q5n` | **13:00 UTC** | |
| 生成日 | `J5n` | **2 = 周二** | 订单生成 |
| 授标日 | `eIn` | **0 = 周日** | 授标 |
| 锁定编辑 | `tIn` | **截止前 24 小时** | |
| 备注长度 | `rIn` | 1024 字符 | |

---

## 二、Tier 系统

```
GET /api/v3/government-orders/tier   →  { tier: { tierIndex, resourceMultiplicator } }
```

| 字段 | 含义 |
|---|---|
| `tierIndex` | 公司档位（显示成 T1/T2/T3…），越高分到的资源份额越大 |
| `resourceMultiplicator` | 该档位对应的资源倍率 |
| `minimumRequiredTierIndex` | 投标的最低档位门槛 |

**tier 同时决定资源量和押金金额。** 🔵 tier 本身怎么算，服务端说了算。

---

## 三、订单模板结构（🟢）

```
{
  id, realm, projectKey, estimatedBaseValue, daysToFulfill,
  resourceMultiplierAwarded,        // 已授标才有值，投标期是 null
  created,
  governmentorderrequiredresourceSet: [
      { id, kind, quality, amountBase }    // quality = 最低品质要求
  ]
}
```

内建项目定义在 `entry.js:36374`（`iIn`），每个项目 `{ agency, enabled, resources: [{weight, kind}] }`。

### 21 个政府机构（🟢）

FIRE_DEPARTMENT 消防局 · NATURAL_DISASTER_RELIEF_AGENCY 灾害救援 · CITY_ZOO 市动物园 ·
CITY_PUBLIC_TRANSPORT 市公交 · PUBLIC_HEALTH_DEPARTMENT 公共卫生 · DEPARTMENT_OF_AGRICULTURE 农业部 ·
ENVIRONMENTAL_PROTECTION_AGENCY 环保署 · ENERGY_DEPARTMENT 能源部 · DEPARTMENT_OF_COMMERCE 商务部 ·
DEPARTMENT_OF_DEFENSE 国防部 · DEPARTMENT_OF_EDUCATION 教育部 · DEPARTMENT_OF_HOUSING 住房部 ·
SPACE_EXPLORATION_AGENCY 航天局 · NATIONAL_FOOD_AGENCY 国家食品局 · FORESTRY_DEPARTMENT 林业局 ·
TRANSPORTATION_SAFETY_BOARD 交通安全委 · ECONOMIC_DEVELOPMENT_AGENCY 经济发展署 ·
MINING_AND_RESOURCES_AGENCY 矿产资源署 · FOREIGN_AFFAIRS_DEPARTMENT 外交部 · TOWN_HALL 市政厅 ·
NATIONAL_ARTS_AGENCY 国家艺术局

---

## 四、资源分配算法 `D1e`（🟢）

```
1. 把投标者按 tierIndex 降序排，取最高档
2. 位置没填满时，用最高档的"假想投标者"补齐
3. 把所有投标者按 tierResourceMultiplicator 升序排
4. 逐个用 Rzi(bidder, allBidders, maxContractors) 算份额：
       档位越高 → computedResourceMultiplicator 越大
       computedResourcesNeeded[kind].amount = max(1, ceil(amountBase × computedResourceMultiplicator))
       computedResourcesNeeded[kind].quality = 模板要求的品质
5. 返回 topTierIndex / topTierResourceMultiplicator / bidderData / totalAmounts / isApplicationComplete
```

> ⚠️ 第 2 步很关键：**空位按最高档填补**，所以人没招满时，每个人分到的量会被算得更多。

---

## 五、押金（🟢）

```
deposit = floor( estimatedBaseValue × resourceMultiplicator × 0.1 )
```
（`Mzi(t, e)`，`Rl.depositRatio = 0.1`）

若投标者记录里 `depositPaid = 0`，UI 就用这条公式实时算。

### 押金相关流水
| 类型 | 代码 |
|---|---|
| 缴纳押金 | `GOVERNMENT_ORDER_DEPOSIT` / `1-godeposit` |
| 押金退回 | `GOVERNMENT_ORDER_DEPOSIT_RETURNED` / `1-godeposit-ret` |
| 押金没收 | `GOVERNMENT_ORDER_DEPOSIT_LOST` / `1-godeposit-lost` |
| 履约收入 | `GOVERNMENT_ORDER_FULFILLED` / `1-gofullfil` |

---

## 六、报酬（🟢）

```
bidderReward = floor( Σ  computedAmount[kind] × bidPricePerUnit[kind] )
computedAmount = max(1, ceil(amountBase × computedResourceMultiplicator))
bidPricePerUnit 来自主承包商设定的 resourcePriceBreakdown（JSON 串）
```

**低价者得**：官方说明就是 "Companies willing to put down deposit can bid, lowest bid wins."
主承包商设的每单位报价越低，中标概率越高。

---

## 七、时间线（🟢）

```
创建 → 投标期 5 天 → 截止前 24 小时锁定参数 → 周日 13:00 UTC 授标
     → daysToFulfill 天履约期

履约总截止 = created + (5 + daysToFulfill) × 24h        （wI 函数）
```

### 履约
```
资源过滤：warehouse.filter(r => r.kind === 要求kind && r.quality >= 要求quality)
进度显示：min(100, 100 × 可用量 / 需要量)
主承包商可以代其他人履约（PATCH .../contractors/{id}）
全员 fulfilled = true → 订单完成 → 押金退回；失败/弃标 → 押金没收
```

---

## 八、投标编辑规则（🟢）

投标创建时设定：`maxContractorCount`(3–7)、`isPublic`（公开列出 / 仅凭链接）、
`resourcePriceBreakdown`、`minimumRequiredTierIndex`。
**截止前 24 小时起，这四项全部锁定不能改。**

加入条件：有空位 + 自己的 tier ≥ `minimumRequiredTierIndex` + 订单公开或持有 secret 链接。

---

## 九、API（🟢）

| 函数 | 端点 | 方法 | 用途 |
|---|---|---|---|
| `gcr` | `api_v3_government_orders` | GET | 列出全部订单 |
| `mcr` | `api_v3_government_orders_get` | GET | 单个模板 |
| `vcr` | `api_v3_government_orders_tier` | GET | 本公司 tier |
| `ycr` | `api_v3_government_orders_applications` | GET | 某模板下的投标 |
| `bcr` | `..._applications_get` | GET | 单个投标 |
| `_cr` | `..._company_applications` | GET | 本公司的投标 |
| `Ecr` | `..._applications` | POST | 创建投标 |
| `lBe` | `..._applications_get` | PATCH | 修改投标参数 |
| `wcr` | `..._applications_get` | DELETE | 删除投标 |
| `xcr` | `..._applications_contractors` | POST | 加入为承包商 |
| `cBe` | `..._applications_contractors_get` | PATCH | 履约 |
| `Scr` | `..._applications_contractors_get` | DELETE | 移除承包商 |

**解锁等级：20**（见《12-等级成就与证书》能力表 `governmentOrders`）。
