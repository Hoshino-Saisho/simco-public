# 05 · 餐馆机制（Restaurant，建筑 `r`）

> 来源：`server/game/restaurant.ts`、`server/domain/restaurant/restaurant-state-machine.ts`、
> `resource_lookups.json dpr.r`、`buildings.json`。
>
> **可信度提示**：餐馆的**数据表**（食材系数、多样性系数、菜品分组、菜单价区间、座位数）
> 在私服代码里明确注明"来自官方客户端 bundle 的餐馆表"（🟢/🟡）；
> 但**评分与上座率的具体公式官方放在服务端，bundle 里没有**（🔵）——
> 本章把两者分开标，别混用。

---

## 一、建筑参数（🟢 官方）

| 项 | 值 |
|---|---|
| dbLetter | `r` |
| 名称 | restaurant 餐馆 |
| 分类 | **sales 销售** |
| sincePhase | 8 |
| costUnits | **26** → 兜底造价 26 × 3450 = **$89,700**/级 |
| buildDuration | 10,800 秒（3 小时） |
| salaryModifier | **1.7** |
| 建造材料（1 级） | 钢筋混凝土(101) 104 / 砖(102) 1430 / 木板(108) 416 / 建造单位(111) 26 |

---

## 二、菜品与分组（🟢 官方数据）

`dpr.r` = 餐馆可用的 16 道菜：

### 沙拉吧 Salad bar（6 道）
| ID | 菜品 | 食材系数 |
|---:|---|---:|
| 117 | Milk 牛奶 | **288** |
| 121 | Bread 面包 | 24.89 |
| 134 | Butter 黄油 | 92.6 |
| 122 | Cheese 奶酪 | 38.196 |
| 119 | Coffee grounds 咖啡粉 | 96.312 |
| 123 | Apple pie 苹果派 | 16.667 |

### 主菜 Mains（6 道）
| ID | 菜品 | 食材系数 |
|---:|---|---:|
| 129 | Hamburger 汉堡 | **3.608** |
| 130 | Lasagna 千层面 | 4.073 |
| 131 | Meatballs 肉丸 | 3.505 |
| 142 | Salad 沙拉 | 9.402 |
| 143 | Samosas 萨莫萨 | 10.093 |
| 149 | Pumpkin soup 南瓜汤 | 9.2 |

### 饮品 Drinks（4 道）
| ID | 菜品 | 食材系数 |
|---:|---|---:|
| 132 | Cocktails 鸡尾酒 | **4.04** |
| 124 | Orange juice 橙汁 | 144 |
| 125 | Apple cider 苹果酒 | 128.955 |
| 126 | Ginger beer 姜汁啤酒 | 113.984 |

> 系数越大 = 每周期消耗越多。主菜和鸡尾酒系数很小（3–10），
> 牛奶/橙汁/苹果酒/姜汁啤酒这几样系数极大（100–288）——这是成本结构的关键。

---

## 三、食材消耗公式（🟡 官方系数 + 已实现）

```
varietyIndex  = clamp(该分组内已选菜品数 − 1, 0, 5)
variety       = RESTAURANT_VARIETY_FACTORS[varietyIndex]
              = [2.1, 1, 0.9, 0.8, 0.8, 0.8][varietyIndex]

seatsPerLevel = 豪华 ? 500 : 1000

需求量 = ceil( size × coefficient × variety × seatsPerLevel / 1000 )
```

**多样性系数是餐馆最重要的一条**：

| 该分组选了几道菜 | 系数 | 说明 |
|---:|---:|---|
| 1 道 | **2.1** | 独苗时每道菜要备 2.1 倍的量 |
| 2 道 | 1.0 | |
| 3 道 | 0.9 | |
| 4 道 | 0.8 | |
| 5 道 | 0.8 | |
| 6 道 | 0.8 | 到顶 |

> 所以"每组只上 1 道菜"是最贵的配法（2.1×），上到 2 道就直接掉到 1.0，
> 上到 4 道以后就不再省了。**每组 4 道是性价比拐点。**

**豪华餐馆的食材需求只有经济型的一半**（500/1000），但座位也只有一半。

---

## 四、座位数（🟡）

```
经济型：seats = size × 1000
豪华型：seats = size × 500
```

---

## 五、工资（🟡 官方常数 + 组合方式已实现）

```
每周期工资 = ceil( 345                          // AVERAGE_SALARY
                 × 1.7                          // 餐馆 salaryModifier
                 × styleMultiplier              // 豪华 0.5 / 经济 1
                 × size
                 × 行政开销（COO 减免后）
                 × 12                           // 周期小时数
                 × staffMultiplier )            // 专业服务员 5 / 普通 1

行政开销 = ph(AO, skillCOO) = AO − (AO − 1) × skillCOO / 100
```
> **"专业服务员/优质服务"把工资直接乘 5**（`PROFESSIONAL_STAFF_WAGE_MULTIPLIER = 5`），
> 换来评分里 `serviceScore` 从 1.25 涨到 2.5。这是餐馆最大的一笔取舍。

---

## 六、周期与状态机（🟡 / ⚪）

| 项 | 值 |
|---|---|
| 一个营业周期 | **43,200 秒 = 12 小时** |
| 菜单价区间 | **$60 – $350** |
| 改装（经济 ⇄ 豪华）耗时 | `size × 10,800 秒`（每级 3 小时） |
| 改装费用 | `ceil(26 × 10 × 345 × size / 2)` = `ceil(44,850 × size)` |
| 评分上限 | 10 |

### 状态机（⚪ 私服形式化，行为对齐官方）

```
状态：Closed（歇业）/ Running（营业中）/ StopScheduled（已排歇业）/ Reconstruction（改装中）

判定：
  reconstruction_until 未到期 或（busy_until 未到期 且 无进行中周期） → Reconstruction
  有进行中周期 → keepOpen ? Running : StopScheduled
  否则 → Closed

合法转移：
  Closed        --START_CYCLE-->          Running
  Closed        --BEGIN_RECONSTRUCTION--> Reconstruction
  Running       --SCHEDULE_STOP-->        StopScheduled
  Running       --CYCLE_SETTLED-->        Running（keepOpen 则自动开下一轮）
  StopScheduled --CYCLE_SETTLED-->        Closed
  StopScheduled --RESUME_CONTINUOUS-->    Running（周期中途取消歇业计划）
  Reconstruction--RECONSTRUCTION_DONE-->  Closed
  Reconstruction--CYCLE_SETTLED-->        ✗ 非法

PATCH keepOpen=true 的语义：
  周期进行中 → 是"取消歇业计划"（RESUME），绝不是开第二轮
  无周期     → 是"开新周期"（START_CYCLE）
只改菜单或改价，不产生任何副作用（不会自动开一轮）。
```

### 开一轮周期的前置条件
1. 建筑是 `r`、属于本公司、`busy_until` 已过期
2. `keepOpen = true`、没有未结算的周期
3. **菜单三个分组各至少一道菜**（沙拉吧 / 主菜 / 饮品）
4. 仓库里三组食材都够（按上面的需求公式）

开局时**一次性扣掉全部食材 + 全部工资**（先付钱，后收账）。

---

## 七、评分与上座率（🔵 服务端逻辑 —— 官方 bundle 里没有）

**重要：评分与上座率的真实公式在官方服务端，前端 bundle 里没有。**
（官方在"学院"面板上有一句 `Building.academyImpactNotDocumented`——"具体影响未做说明"，
那是学院不是餐馆，但同样说明这类模拟量官方一律放服务端、前端不给公式。）

下面是私服 `game/restaurant.ts` 的自定实现，**结构合理但数值不代表官方**：

### 评分（满分 10）
```
前置：菜单非空 且 三个分组都有菜，否则 rating = 0

qualityScore   = clamp(菜单平均品质 / 12, 0, 1) × 3
varietyScore   = clamp(菜品数 / 16, 0, 1) × 1.5
serviceScore   = 优质服务 ? 2.5 : 1.25
luxuryScore    = 豪华 ? 0.5 : 0
priceScore     = 价格 ≤ 96 ? 0.5 : clamp(0.5 − (价格 − 96)/508, 0, 0.5)
communication  = clamp(CMO 沟通技能, 0, 100) × 0.01
sales          = 有销售加成 ? 0.02 : 0

rating = clamp(以上七项之和, 0, 10)
```

### 上座率
```
pricePenalty = max(0, 价格 − 96) / 500
competition  = clamp(本公司其它在营餐馆座位数 / 市场客流, 0, 0.55)

occupancy = clamp( 0.08 + rating/10 × 0.82 − pricePenalty − competition × 0.25, 0, 1 )
```

### 结算
```
served  = min(已备餐量, floor(座位数 × occupancy))
spoiled = 已备餐量 − served                      ← 备多了就是浪费，没有回收
revenue = served × 菜单价
profit  = revenue − (食材成本 + 工资)
歇业惩罚：keepOpen=false 结算时 rating ×= 0.875
结算后若 keepOpen 且条件满足，自动接着开下一轮
```

> **$96 这个数在评分和上座率里都出现**：菜单价 ≤ $96 拿满分价格分且无上座惩罚，
> 超过就双重扣。私服把它设成了一个明显的甜点位。官方是否也是 96，无法从 bundle 验证。

---

## 八、报纸里的餐馆图表（🟢 官方字段）

官方报纸有两种餐馆文章类型，说明服务端确实在跟踪这些量：
- `RESTAURANT_HEATMAP` —— 上座率热力图
- `RESTAURANT_DISTRIBUTION` —— 按评分的餐馆分布

零售历史图表里也有 `retailData[].amountSoldRestaurant` 字段（餐馆单独统计的销量）。
