# 03 · 生产机制与公式

> 来源：`formulas_production.md`（🟢）、`registry/encyclopedia-formulas.json`（🟢，含官方截图数值验证）、
> `resource_lookups.json`、`server/game-data/buildings.ts`。

---

## 一、完整生产链（一步一步）

### 第 1 步 · 工资水平修正后的基础产量

```
base = producedPerHourRaw × (AVERAGE_SALARY / SALARY_MID[economyState]) ^ salaryModifier
     = producedPerHourRaw × (345 / SALARY_MID[state]) ^ salaryModifier

SALARY_MID = { 0: 655（衰退）, 1: 700（正常）, 2: 745（繁荣） }
salaryModifier 取【生产建筑】的值，不是资源的
```

> bundle 锚点：`function oNt(e,t,r){return e*Math.pow(ea.AVERAGE_SALARY/t,Qr[r].salaryModifier)}`
> **官方截图验证**：苹果 `250 × (345/700)^0.3 = 202.1884417437` → 页面显示 `202.19`。✅
> （其余两种经济状态都凑不出这个数，所以截图对应的是 state 1。）

### 第 2 步 · 品质修正（**只对采矿类**）

```
if isMining(resource):  base ×= quality / 100
```

采矿类资源清单（🟢 `chunk_oP.js:43-54` 的 `px()`；私服 `MINING_RESOURCE_KINDS` 与之一致）：

| ID | 资源 |
|---:|---|
| 10 | Crude Oil 原油 |
| 14 | Minerals 矿物 |
| 15 | Bauxite 铝土矿 |
| 42 | Iron Ore 铁矿石 |
| 44 | Sand 沙子 |
| 68 | Gold Ore 金矿石 |
| 74 | Methane 甲烷 |
| 104 | Clay 黏土 |
| 105 | Limestone 石灰石 |

**其余所有资源，品质不影响产量**，只影响零售速度/售价和交易所撮合优先级。
（注意这里的 `quality` 是 0–100 的百分数口径，采矿建筑的"丰度 abundance"就走这一路。）

### 第 3 步 · 事件加速

```
base ×= (1 + activeEvent.speedModifier / 100)
```
事件来自 `GET /api/v2/production-modifiers/{realmId}/`，结构 `{kind, speedModifier, since, until}`，
按资源 dbLetter 匹配、按 UTC `since/until` 判定是否生效。
持续天数 `PRODUCTION_SPEED_MODIFIER_DAYS = 21`，同时最多 `PRODUCTION_SPEED_MODIFIER_RESOURCES = 3` 个资源。

### 第 4 步 · 建筑等级 + 加成总合

```
totalBonus% = productionModifier      （公司生产加成，SimBoost/等级奖励）
            + recreationBonus          （休闲建筑加成，见第 13 章）
            + accumulatorQualityBonus  （只对累加器资源）

ratePerHour = size × base / (1 − totalBonus / 100)
```

> bundle 锚点 `function sNt(e,t,r,n,i,a,o){ ... return i*c/(1-u/100) }`。
> **注意分母形式**：加成是"除以 (1 − x)"而不是"乘以 (1 + x)"。
> 100% 加成 = 无穷大，所以加成实际有软上限（私服把分母夹在 ≥ 0.25，即最多 4 倍）。

### 第 5 步 · 机器人（**只对累加器建筑**）

```
effectiveSalary% = salaryPercent% + 4 × robotCount     （lsr = 4）
ratePerHour = size × base / (1 − effectiveSalary / 100)
```
> `chunk_Unn.js:213`。**非累加器建筑，机器人不加产量**，只省 3% 工资 + 锁定单品。

### 第 6 步 · 研发类资源额外加成

```
if isResearch:  ratePerHour ×= (100 + 有效CTO技能 × 2) / 100      （hb = 2）
```

### 第 7 步 · 生产耗时

```
每单位耗时(秒) = 345 × 建筑salaryModifier × size / ratePerHour × eventMultiplier
eventMultiplier = 0.97（活动期）或 1

排队耗时 = max(0, spareCapacity − 1) × 单位耗时
spareCapacity = ph(行政开销, COO技能) = AO − (AO − 1) × skillCOO / 100
```

---

## 二、一句话汇总公式

```
ratePerHour =
    size
  × producedPerHourRaw
  × (345 / SALARY_MID[state]) ^ buildingSalaryModifier
  × (isMining ? quality/100 : 1)
  × (1 + eventSpeedModifier/100)
  ÷ (1 − (productionModifier + recreationBonus + accumulatorBonus + isAccumulator×4×robots) / 100)
  × (isResearch ? (100 + ctoSkill×2)/100 : 1)

单位人工成本 = (345 × buildingSalaryModifier) / ratePerHour
单位管理成本 = max(0, ph(AO, skillCOO) − 1) × 单位人工成本
```

---

## 三、累加器机制（Accumulator）🟢

全游戏**只有 #150 圣诞树（Tree）**用这套机制，生产建筑是苗圃 `v`。

```
参数（resources.json #150 productionMechanic.accumulatorParameters）：
    baseValue        = 10
    max              = 81910
    amountPerLevel   = 1
    bonusPerQuality  = 4     （%）

累加值阈值：  threshold(q) = baseValue × 2^q = 10 × 2^q
当前品质：    quality = floor( log2( currentValue / baseValue ) )，夹在 [0, floor(log2(max/base))]
            → 最高品质 = floor(log2(81910/10)) = floor(log2(8191)) = 12
生产加成：    accumulatorQualityBonus = bonusPerQuality × 已研究品质 = 4 × q  （百分点）
```
> bundle 锚点 `function w0e(e,t){return e*bln(2,t)}`、`Eur=4`；私服 `game-data/accumulator.ts` 一致。
> 累加值阈值序列：10, 20, 40, 80, 160, 320, 640, 1280, 2560, 5120, 10240, 20480, 40960（上限 81910）。

---

## 四、季节对生产的影响 🟢

只有 `productionSeason` 非空的资源受影响，目前只有一个季节事件：

```
AutumnHarvest 秋收：9 月 3 日 – 11 月 14 日，productionYield 上限 1.0 → 1.5
```

判定函数 `n9r(resource, date)`：
```
1. 无 productionSeason → 恒为 true
2. 在 productionYield 折线上按日期线性插值：
       progress   = 已过天数 / 两个锚点之间的天数
       currentMax = (maxB − minA) × progress + minA
3. return currentMax > 0.8    （G9 阈值）
```
UI 上非当季资源显示的产出区间是 **[0.03, 1.4]**。

---

## 五、采矿丰度（Abundance）🔵 服务端逻辑

官方把丰度算在服务端，前端只通过 `GET /api/v2/companies/buildings/:id/abundance/`
拿 `{buildingId, abundance, originalAbundance}` 三个字段（🟢 这个接口契约是官方确证的）。
**丰度怎么滚、怎么衰减，bundle 里没有。**

私服 `server/game/buildings.ts` 的自定实现（⚪ 仅供参考，不是官方）：
```
带丰度的建筑：矿场 M、采石场 Q、石油钻井 O
建造时滚一次：clamp(Gaussian(μ=0.85, σ=0.15), 0.5, 1.0) × 100  → 落在 [50, 100]
产出缩放：    产量 × abundance / 100
每完成一个生产周期衰减：abundance ×= (1 − 0.00032)
重新勘探（prospect）：重滚一次，originalAbundance 一起重置
```

---

## 六、生产队列与时限

| 项 | 值 | 来源 |
|---|---|---|
| 队列时长上限 | L0–4：**2 小时**；L5–14：**24 小时**；L15+：**48 小时** | `domain/leveling/level-rules.ts` `timeLimitS`（🟡） |
| 发射队列 | 最多 30 条，且**不受上面的时限约束**（128h 发射本身就超过所有 tier 上限） | `LAUNCH_QUEUE_MAX = 30`（🟢） |
| 建筑忙碌 | `busyEndTimestamp = Date.parse(busy.started) + busy.duration × 1000` | `entry.js:56045-56052`（🟢） |

---

## 七、机器人对工资的影响

```
所需数量：  ceil(每级机器人数 × size)，每级公式见《02-建筑全量数据》2.5   🟢
所需品质：  floor(size / 4)                                              🔵
副作用：    锁定单一产品；装着机器人不能升降级；卸载返还 50%（Q0）        🔵
装机器人后：工资 × 0.97（省 3%）                                          ⚪ 私服自定
```
> ⚪ 那条 0.97 只出现在私服 `game/robotics.ts`。decompile 里的 0.97 是**活动期倍率**，两码事。
